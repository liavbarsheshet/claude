# External Preferences

Learn the following preferences from these external sources and merge them into `~/.claude/CLAUDE.md`.
Each entry can be a URL, a GitHub repo, or a shell command (e.g. `npx ...`):

-
