# External Hooks

Learn the following hooks from these external sources, copy the scripts to `~/.claude/hooks/`, and register them in `~/.claude/settings.json`.
Each entry can be a URL, a GitHub repo, or a shell command (e.g. `npx ...`):

-
