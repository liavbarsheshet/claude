# External Commands

Learn the following commands from these external sources and install them into `~/.claude/commands/`.
Each entry can be a URL, a GitHub repo, or a shell command (e.g. `npx ...`):

-
